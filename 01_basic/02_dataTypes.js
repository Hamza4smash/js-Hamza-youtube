"use Strict;" //treat all JS code as  of newer version


// alert(3+3) // we are using node.js, not browser 



let name ="Hamza"
let age=25;
let isLoggedIn=false
let state= null
 // Primitive Data Types in JS

// number => range: -(2^53 - 1) to +(2^53 - 1)
let num = 42;

// bigint => for very large integers
let bigIntVal = 123456789012345678901234567890n;

// string => sequence of characters
let str = "Hello Hamza";

// boolean => true/false values
let isActive = true;

// null => intentional empty value, //standalone value,, //null is  an Object
let emptyVal = null;

// undefined => variable declared but not assigned
let notAssigned;  //it is a type i.e undefine

//symbol => unique and immutable value

//object

console.log(typeof undefined);
